import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ortahisar.demirbasapp',
  appName: 'Ortahisar Demirbas Takip',
  webDir: 'build'
};

export default config;
